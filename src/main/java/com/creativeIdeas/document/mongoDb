Flexible, schema-less data (e.g. comments, nested replies)
Fast writes and large volumes (feedback, drafts).
Dynamic content (drafts evolve often, can be saved mid-edit).

'Spring Data MongoDB' similar like JPA but for MongoDB